import './modulestyle.css';

function SettingsButton () {
  const handleClick = () => {
    
  };
  return (
    <button id="SettingsButton" className="Button" onClick={handleClick}>
      Settings
    </button>
  );
}

export default SettingsButton;
